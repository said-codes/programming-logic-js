let numeroSecreto = 6;
let numeroUsuario = prompt("Me indicas un numero por favor:");

console.log(numeroUsuario);

if (numeroUsuario == numeroSecreto) {
    alert('Acertaste el numero');
}

//desafio
/*
// Muestra una alerta de bienvenida
alert("¡Bienvenida y bienvenido a nuestro sitio web!");

// Declaración de variables
let nombre = "Lua";
let edad = 25;
let numeroDeVentas = 50;
let saldoDisponible = 1000;

// Muestra una alerta con mensaje de error
alert("¡Error! Completa todos los campos");

// Declaración de la variable mensajeDeError
let mensajeDeError = "¡Error! Completa todos los campos";
alert(mensajeDeError);

// Uso de prompt para preguntar el nombre del usuario
nombre = prompt("¿Cuál es tu nombre?");

// Solicitar la edad del usuario con prompt
edad = parseInt(prompt("¿Cuántos años tienes?"), 10);

// Verificar si la edad es mayor o igual a 18
if (edad >= 18) {
    alert("¡Puedes obtener tu licencia de conducir!");
} else {
    alert("Aún no puedes obtener tu licencia de conducir.");
}
*/
